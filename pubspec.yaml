name: minecraft_flutter_launche
description: "A new Flutter project."

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  sidebarx: ^0.17.2
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  file_picker: ^8.1.6
  crypto: ^3.0.3
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/background.jpg
